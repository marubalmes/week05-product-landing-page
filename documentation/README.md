# Documentation Assets

Save the required before-and-after comparison here:

- `before-design.png`
- `after-design.png`

The Week 5 PDF requires the before version to show the initial wireframe/basic layout and the after version to show the polished responsive interface.
